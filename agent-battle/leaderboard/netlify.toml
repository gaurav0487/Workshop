[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[dev]
  edge_functions = false

# SPA fallback — functions with config.path take priority automatically
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
